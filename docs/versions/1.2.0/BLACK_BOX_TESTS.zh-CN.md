# ReadyType 1.2.0 黑盒功能检查

最后更新：2026-07-13。当前候选构建：`1.2.0 (80)`。

## 需求对应与状态

| 产品项 | 状态 | 验证证据 |
| --- | --- | --- |
| 主窗口信息架构 | 已完成 | 侧栏拆分为使用概览、首页、常用词、语言与输出、快捷键、语音识别、权限与隐私、关于。 |
| 浅色、深色、跟随系统 | 已完成 | 三种外观均在真实窗口切换检查；跟随系统在深色系统下正确显示深色。 |
| Dashboard | 已完成自动化与视觉验证 | 聚合存储、同日合并、连续天数、清除统计测试通过；空状态与真实数据状态视觉通过。 |
| Dashboard 隐私 | 已完成 | 持久化文件只包含每日数值，测试确认不包含转写或输出正文。 |
| 简繁体输出 | 已完成 | 简体、繁体、英文与数字保留、粘贴前统一转换测试通过。 |
| 高精度状态点 | 已完成 | 已准备好时显示绿色；录音、处理中和错误状态保持优先。 |
| 菜单栏关闭 | 已完成用户验收 | transient popover、外部点击、图标切换和 Esc 关闭已收口；用户反馈无明显问题。 |
| 核心语音输入 | 自动化与真实语音通过 | 录音、识别、AI 处理、粘贴和复制降级测试通过；构建 80 在文本编辑完成真实语音粘贴。 |
| 常用词端到端生效 | 自动化与真实语音通过 | 常用词进入极速识别、高精度 Whisper 和 AI 整理；Typeless、Reddit 的真实语音保留标准写法。 |

## 已执行验证

- `swift test`：357 项执行，14 项按真实服务或环境条件跳过，0 失败。
- `scripts/build-app.sh`：通过；App 版本为 `1.2.0 (80)`。
- 常用词候选超时测试：修复取消后继续计算的问题，本机回退耗时约 82-85ms。
- GitHub Release workflow：YAML 解析与发布门禁测试通过；仅在版本 tag 与 App 版本一致时发布 ZIP、DMG 和 SHA-256 校验文件。
- 紧凑 HUD：真实录音状态全屏截图通过，尺寸缩小且标题、输出方式、计时、波形和取消提示均可见。
- `scripts/package-app.sh`、`scripts/package-dmg.sh` 和 `hdiutil verify`：通过；ZIP 与 DMG 已重新生成。
- Dashboard 深色、浅色、跟随系统：真实窗口检查通过。
- Dashboard 真实数据状态：完成输入次数、语音时长、字数和节省时间均显示实际聚合值。
- 敏感信息与 `git diff --check`：发布提交前执行。

## 最终人工确认

1. 已抽查 Dashboard 持久化文件，只包含日期、次数、语音秒数和字符数，不含识别或输出正文。
2. 菜单栏关闭行为沿用此前已通过用户验收的实现；构建 80 未修改菜单栏代码，相关自动化继续通过。
3. “清除统计”确认框已打开并取消，18 次本机统计完整保留，未执行删除。
4. 已在构建 80 真实口述 Typeless / Reddit 测试句；原始识别和最终输出写法正确，中文全角标点正确。

## 发布阻塞项

1. 提交最终发布文档并等待远端 CI 通过。
2. 创建 `v1.2.0` tag，并核对自动生成的 GitHub Release 下载资产。
