# ReadyType 1.5.1 黑盒功能检查

## 当前状态

识别与输出链路改进已通过本地和远端发布门禁，`v1.5.1` 已公开发布；选中文字语音操作仍不可用。

| 需求 | 状态 | 证据 |
| --- | --- | --- |
| 长句并行识别 | 完成 | 回归测试覆盖 8 秒边界、并行启动、3 秒预算和极速结果复用。 |
| 有界质量选择 | 完成 | 已覆盖各引擎质量元数据和完整候选选择，不直接比较跨引擎原始分数。 |
| 上下文词与 AI 延迟 | 完成 | 已覆盖有限规范词、通用拼写要求、关闭思考、限制输出及分段耗时展示。 |
| 自动化与打包门禁 | 完成 | 439 项全量测试通过，14 项外部环境测试按设计跳过；正式构建、签名检查、UI 门禁、敏感信息扫描、ZIP、DMG 和校验文件均通过本地及 GitHub Actions 验证。 |

## 已验证候选

- 应用：`dist/ReadyType.app`
- 版本：`1.5.1 (96)`

## 发布验证

- GitHub Release 工作流 [31860207850](https://github.com/whnnick/readytype/actions/runs/31860207850) 已通过版本校验、测试、正式统计配置、构建、敏感信息扫描、打包和发布。
- 公开 [v1.5.1](https://github.com/whnnick/readytype/releases/tag/v1.5.1) 已成为 latest，且不是草稿或预发布；包含 `ReadyType.app.zip`、`ReadyType.dmg` 和 `SHA256SUMS.txt`。
- 重新下载的 ZIP 与 DMG 均通过 `SHA256SUMS.txt`；ZIP 内应用为 `1.5.1 (96)` 并通过严格签名验证，DMG 通过 `hdiutil verify`。

## 发布阻塞

- 1.5.1 无阻塞。独立的 1.6.0 选中文字流程仍在开发中。
